version: '3.7'
services:
  jenkins:
    image: maxiplux/jenkins:latest
    
    restart: always
    privileged: true
    user: root
    ports:
      - 8080:8080
      - 50000:50000
    container_name: jenkins
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
